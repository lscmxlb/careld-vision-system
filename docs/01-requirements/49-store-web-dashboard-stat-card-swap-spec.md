# store-web 数据中心统计卡 1/2 对调 Spec（第 102 项）

| 项目 | 内容 |
|---|---|
| 文档类型 | 前端版面调整 Spec（统计卡片顺序对调） |
| 来源 | 用户需求：http://39.162.49.28:5175/dashboard 顶部第 1 张「本月预约/总预约数量」与第 2 张「本月新增/档案总数」两张卡片内容对调（编号顺延自拟为第 102 项） |
| 涉及端 | store-web（5175）`/dashboard` 数据中心 |
| 状态 | 已开发（2026-09-19，真机 DOM 断言通过，vue-tsc EXIT=0） |

---

## 1. 需求清单

| # | 子项 | 说明 |
|---|---|---|
| 102-1 | 卡片对调 | 第 1 张「本月预约/总预约数量」与第 2 张「本月新增/档案总数」整体对调位置（含图标与配色） |
| 102-2 | 其余不变 | 第 3 张「本月养护/总养护数量」及以下区域保持原样 |

---

## 2. 现状诊断

- 顶部统计行三张 `el-col :span="8"` 卡片顺序为：① 本月预约（蓝 `#e6f7ff`/Calendar）→ ② 本月新增（橙 `#fff7e6`/User）→ ③ 本月养护（绿 `#f6ffed`/Monitor）。
- 对调仅涉及模板中两个 `el-col` 块的顺序，数据绑定、图标、样式均随卡片整体移动，无需改脚本。

---

## 3. 改造设计

`frontend/store-web/src/views/dashboard/index.vue` 顶部统计行：

- 将「本月新增/档案总数」（User 图标、`#fff7e6`/`#faad14`）块移至第 1 位；
- 「本月预约/总预约数量」（Calendar 图标、`#e6f7ff`/`#1890ff`）块移至第 2 位；
- 第 3 张「本月养护/总养护数量」不动。

无脚本、类型、接口改动。

---

## 4. 验证计划与结果（2026-09-19 真机）

| 手段 | 结果 |
|---|---|
| `vue-tsc --noEmit`（store-web） | ✅ EXIT=0 |
| 真机 DOM 断言（`http://39.162.49.28:5175/dashboard`，store001_mgr 登录，硬刷新后整页渲染再断言） | ✅ 统计行 3 张卡顺序为：① `本月新增 8 / 档案总数 12`（图标背景 `rgb(255,247,230)` 橙）→ ② `本月预约 10 / 总预约数量 17`（图标背景 `rgb(230,247,255)` 蓝）→ ③ `本月养护 1 / 总养护数量 1`（绿），三卡均 `el-col-8` |
| 控制台 | ✅ 无新增错误（仅历史既有告警：vue-router `next()` 弃用、el-radio `label` 弃用 ×2、ECharts 0×0 视口） |

---

## 5. 遗留与说明

1. 门店 7（用户截图门店，东湖中里社区卫生服务中心）账号密码非通用值且不得重置，UI 级验证在门店 1（store001_mgr）完成；数据结构/顺序与门店无关。
2. 应用内浏览器为 0×0 隐藏视口，以 DOM 顺序与图标配色断言为准，最终视觉效果以用户截图确认为准。
3. 编号自拟：本项用户未给出编号，按序列记为 #102。
4. 与 #63–#101 同属未提交改动，等待确认后一并入库。
