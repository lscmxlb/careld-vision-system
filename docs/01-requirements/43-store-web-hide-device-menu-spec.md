# store-web 隐藏「设备管理」菜单项 Spec（第 96 项）

| 项目 | 内容 |
|---|---|
| 文档类型 | 前端 UI 隐藏 Spec（仅隐藏菜单项，页面/路由/数据链路保留） |
| 来源 | 用户需求：http://39.162.49.28:5175/device 医院端，隐藏「设备管理」这一项（编号顺延自拟为第 96 项） |
| 涉及端 | store-web（5175）侧边栏「系统设置」组 |
| 状态 | 已开发（2026-09-19，真机 DOM 断言通过，vue-tsc EXIT=0） |

---

## 1. 需求清单

| # | 子项 | 说明 |
|---|---|---|
| 96-1 | 隐藏菜单项 | 医院端左侧菜单「系统设置 → 设备管理」不再展示 |

---

## 2. 现状诊断

- 侧边栏为 `frontend/store-web/src/layouts/MainLayout.vue` 中硬编码的 `el-menu`，「设备管理」是「系统设置」子菜单（仅店长可见，`v-if="isManager"`）下的第 3 项，跳转 `/device`。
- 路由 `router/index.ts` 中存在 `device` 路由（`meta.managerOnly`），页面 `views/device/index.vue` 与 `api/device.ts` 均正常工作；除侧边栏外无其它入口链接。

---

## 3. 改造设计

- 删除 `MainLayout.vue` 中一行菜单项声明：`<el-menu-item index="/device">设备管理</el-menu-item>`。
- 与项目既有「隐藏 UI、保留数据链路」先例（spec 15 / #53 家族病史）保持一致：路由、页面组件、接口与数据均不动，后续如需恢复展示只需加回该行。

---

## 4. 验证计划与结果（2026-09-19 真机）

| 手段 | 结果 |
|---|---|
| `vue-tsc --noEmit`（store-web） | ✅ EXIT=0 |
| 真机 DOM 断言（`http://39.162.49.28:5175/schedule`，store001_mgr） | ✅ 侧边栏菜单项为 `数据中心 / 儿童档案 / 养护记录 / 预约记录 / 预约管理 / 排班设置 / 基础信息 / 医务人员 / 日志记录`，**无「设备管理」**（`.el-menu-item[index="/device"]` 查询为空） |
| 直接访问 `/device` | ✅ 页面仍可渲染（`绑定设备 / 设备编码 / 设备名称 / 状态 / 校准 / 最后同步 / 操作`），即本次仅隐藏菜单入口 |

---

## 5. 遗留与说明

1. 「隐藏」范围为菜单入口：直接输入 URL `http://39.162.49.28:5175/device` 仍可访问该页；如需连同路由一并封禁（重定向或 404），请告知后再改。
2. 与 #63–#95 同属未提交改动，等待确认后一并入库。
