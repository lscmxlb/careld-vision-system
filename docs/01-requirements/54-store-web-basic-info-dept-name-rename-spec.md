# store-web 基础信息页「科室名称 → 诊疗项目」改名换位 Spec（第 105 项，编号自拟）

| 项目 | 内容 |
|---|---|
| 文档类型 | 前端 Spec（标签改名 + 表单行对调，字段与接口不变） |
| 来源 | 用户需求（承接 #98，未给编号，按序列自拟为第 105 项）：http://39.162.49.28:5175/basic-info 「科室名称修改为诊疗项目，上下位置和科室类型对调」 |
| 涉及端 | store-web（5175）`/basic-info` |
| 状态 | 已开发（2026-09-20，浏览器 DOM 断言 + UI 保存 DB 对账通过，vue-tsc EXIT=0） |

---

## 1. 需求清单

| # | 子项 | 说明 |
|---|---|---|
| 105-1 | 改名 | 「科室名称」标签改为「诊疗项目」（含输入框 placeholder 与必填校验提示） |
| 105-2 | 换位 | 「诊疗项目」与「科室类型」上下对调 |

调整后行序：医院名称 → **科室类型 → 诊疗项目** → 服务电话 → 收费标准（其余行不变）。

---

## 2. 现状诊断

- #98 改造后该页行序为：医院名称 → 科室名称 → 科室类型 → 服务电话 → 收费标准。
- 仅展示标签与顺序变化，底层字段（`dept_name`）、校验属性（`prop="deptName"`）、保存载荷与接口均无需改动。

---

## 3. 改造设计

`frontend/store-web/src/views/basic-info/index.vue`：

| 项 | 改动 |
|---|---|
| 模板 | 两个 `el-form-item` 块整体对调；标签「科室名称」→「诊疗项目」，placeholder「请输入科室名称」→「请输入诊疗项目」 |
| 校验规则 | `deptRules.deptName` 提示语同步改为「请输入诊疗项目」 |
| 不变项 | `prop="deptName"`、`v-model="deptForm.deptName"`、保存载荷字段、接口、DB 列 `store_department.dept_name` 均保持原样 |

---

## 4. 验证计划与结果（2026-09-20 真机）

| 手段 | 结果 |
|---|---|
| `vue-tsc --noEmit`（store-web） | ✅ EXIT=0 |
| DOM 行序断言（/basic-info） | ✅ 医院名称(0) → 科室类型(1) → 诊疗项目(2) → 服务电话(3) → 收费标准(4)，页面无「科室名称」标签 |
| 输入框 | ✅ placeholder「请输入诊疗项目」，值回显「视光门诊」（仍绑定 `deptName`） |
| UI 保存回归 | ✅ 改为「视光门诊验证X」保存 → DB `dept_name` 落库，`dept_code/service_phone/charge_standard` 未受影响 → UI 改回「视光门诊」保存 → DB 完全复原 |

---

## 5. 遗留与说明

1. 仅医院端（store-web）改名；**运营端 admin-web「科室管理」页仍为「科室名称」**（用户本次明确只改医院端基础信息页），如需同步可另提。
2. 编号自拟：本项用户未给编号，按序列记为 #105（自拟序列当前至 #105）。
3. 与 #63–#104 同属未提交改动，等待确认后一并入库。
