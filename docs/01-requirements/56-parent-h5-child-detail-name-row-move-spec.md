# parent-h5 儿童档案详情「儿童姓名」行下移至主治医师下方 Spec（第 107 项，编号自拟）

| 项目 | 内容 |
|---|---|
| 文档类型 | 前端 Spec（表单行顺序调整，字段与接口不变） |
| 来源 | 用户需求（承接 #106，未给编号，按序列自拟为第 107 项）：「这个儿童档案的页面，将儿童姓名这一行的位置下移至主治医师的下面」 |
| 涉及端 | parent-h5（5177）`pages/child/detail` |
| 状态 | 已开发（2026-09-20，浏览器 DOM 行序断言通过，vue-tsc EXIT=0） |

---

## 1. 需求清单

| # | 子项 | 说明 |
|---|---|---|
| 107-1 | 下移 | 「档案信息」卡片中「儿童姓名」行从原第 2 行下移到「主治医师」的下一行 |

调整后行序：档案编号 → 建档医院 → 服务电话 → **主治医师 → 儿童姓名** → 性别 → 出生日期 → 家长姓名 → 关系 → 手机号码 → 所在学校 → 家庭地址。

---

## 2. 现状诊断

- #106 完成后「档案信息」行序为：档案编号 → 儿童姓名 → 建档医院 → 服务电话 → 主治医师 → 性别 → …。
- 「儿童姓名」行绑定 `child.name` 且带性别着色类（`:class="genderNameClass(child.gender)"`，男孩蓝 `name-boy` / 女孩粉 `name-girl`），移动时须整行搬运保留该绑定。

---

## 3. 改造设计

`frontend/parent-h5/src/pages/child/detail.vue`：模板中「儿童姓名」`<view class="kv">` 整块由「档案编号」之后移动到「主治医师」之后，其余行不动；无脚本、样式、接口改动。

---

## 4. 验证计划与结果（2026-09-20 真机）

| 手段 | 结果 |
|---|---|
| `vue-tsc --noEmit`（parent-h5） | ✅ EXIT=0 |
| dev server 产物核对 | ✅ `curl /src/pages/child/detail.vue` 转译产物中行序为 建档医院 < 主治医师 < 儿童姓名（rsync 后已 touch 强制重编译） |
| 浏览器 DOM 断言（5177，child id=45 王小刚） | ✅ 行序：档案编号 → 建档医院(北京朝阳门店) → 服务电话(—) → 主治医师(—) → **儿童姓名(王小刚)** → 性别 → …；「儿童姓名」值仍带 `name-boy` 性别着色类 |
| 数据影响 | ✅ 纯模板顺序调整，未触碰任何数据（无需复原） |

注：hash 相同地址的导航不会重载文档，断言前需整页 reload 才能拿到新产物（HMR 在隐藏视口下不可靠）。

---

## 5. 遗留与说明

1. 仅调整 parent-h5 档案详情页行序；其它端（doctor-h5 / parent-web / store-web）的档案信息未变动。
2. 编号自拟：本项用户未给编号，按序列记为 #107（自拟序列当前至 #107）。
3. 与 #63–#106 同属未提交改动，等待确认后一并入库。
