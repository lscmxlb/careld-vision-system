# store-web 数据中心去掉「今日检测」卡片、三卡等宽铺满 Spec（第 100 项）

| 项目 | 内容 |
|---|---|
| 文档类型 | 前端版面调整 Spec（删除卡片 + 三卡自适应铺满） |
| 来源 | 用户需求：http://39.162.49.28:5175/dashboard 去掉顶部「今日检测」卡片，其余三个框等比例放大占满整行（编号顺延自拟为第 100 项） |
| 涉及端 | store-web（5175）`/dashboard` 数据中心 |
| 状态 | 已开发（2026-09-19，真机 DOM 断言通过，vue-tsc EXIT=0） |

---

## 1. 需求清单

| # | 子项 | 说明 |
|---|---|---|
| 100-1 | 删除卡片 | 去掉顶部第 4 张「今日检测」统计卡 |
| 100-2 | 三卡铺满 | 剩余「本月预约/总预约数量」「本月新增/档案总数」「本月养护/总养护数量」三张卡等宽（各占 1/3）铺满整行 |

---

## 2. 现状诊断

- 顶部统计行为 4 个 `el-col :span="6"`（各占 1/4），第 4 张为单指标「今日检测」（`stats.todayTests`，紫色 `View` 图标）。
- 页面样式里 `.stat-info` 仅被该卡使用。

---

## 3. 改造设计

1. 模板：删除第 4 张「今日检测」卡片；其余三张 `el-col :span="6"` → `:span="8"`（3 × 8 = 24，等宽铺满）。
2. 脚本：移除随之失效的 `View` 图标引入（`stats.todayTests` 仍由接口返回，保留在初始化对象中，类型不变）。
3. 样式：删除已无引用的 `.stat-info` 规则。

---

## 4. 验证计划与结果（2026-09-19 真机）

| 手段 | 结果 |
|---|---|
| `vue-tsc --noEmit`（store-web） | ✅ EXIT=0（无未使用变量报错） |
| 真机 DOM 断言（`http://39.162.49.28:5175/dashboard`，store001_mgr 登录） | ✅ 顶部统计行恰有 3 个 `el-col`，类名均为 `el-col-8`（各占 1/3）；渲染文本为 `10/本月预约/17/总预约数量`、`8/本月新增/12/档案总数`、`1/本月养护/1/总养护数量`；页面文本不再含「今日检测」 |
| 其余区域回归 | ✅ 趋势图、底部「最近预约 / 养护记录」双卡及列表数据正常渲染（倒序与状态文案同 #99） |
| 控制台 | ✅ 无新增错误（仅历史存在的 vue-router next() / el-radio label 弃用告警与 0×0 视口 ECharts 告警） |

---

## 5. 遗留与说明

1. 应用内浏览器为 0×0 隐藏视口，无法量化实际像素宽度；本次以栅格类名（3 × `el-col-8`）证明等宽铺满，最终视觉效果以用户截图确认为准。
2. 后端 `DashboardStats.todayTests` 字段与接口未删除（仅页面不再展示），后续如需恢复该卡片可直接复用。
3. 编号自拟：本项用户未给出编号，按序列记为 #100。
4. 与 #63–#99 同属未提交改动，等待确认后一并入库。
