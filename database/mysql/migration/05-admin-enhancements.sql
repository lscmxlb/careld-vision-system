-- ============================================================
-- Careld 视力养护系统 - 管理后台增强迁移脚本
-- 日期: 2026-08-08
-- 说明: 设备使用寿命改为维护日期，门店增加机构性质字段
-- ============================================================

SET NAMES utf8mb4;
USE careld_vision;

-- ============================================================
-- 1. 设备表: 使用寿命(月) → 维护日期
-- ============================================================
ALTER TABLE store_tv_device DROP COLUMN service_life_months;
ALTER TABLE store_tv_device ADD COLUMN maintenance_date DATE DEFAULT NULL COMMENT '维护日期' AFTER app_version;

-- ============================================================
-- 2. 门店表: 增加机构性质字段
-- ============================================================
ALTER TABLE store_info ADD COLUMN institution_type TINYINT DEFAULT NULL COMMENT '机构性质:1公立医疗机构 2民营医疗机构 3其他' AFTER bed_count;

-- ============================================================
-- 完成
-- ============================================================
SELECT '✅ 迁移脚本执行完成' AS result;
